{
  "name": "stonk",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A historical market replay sandbox with paper trading, causal bots, and a play-credit arcade.",
  "scripts": {
    "start": "npm run build && npm run preview",
    "build": "node scripts/build.js",
    "preview": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "check": "npm test && npm run build && npm run test:e2e",
    "format": "prettier --check \"web/*.{js,css,html}\" \"scripts/*.js\" \"tests/**/*.js\" playwright.config.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
